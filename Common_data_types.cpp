#include <iostream>

int main() {
    int x = 2; //stores whole numbers without decimals
    double y = 5.14; //stores nombers with decimals
    char z = 'D'; //stores single characters
    std::string i = "Hello"; //stores words or whole sentences
    bool j = true; //stores just tow variables true/false or 1/0
    return 0;
}